/*
const somar = (x,y) =>{
    return x+y;
}

console.log(somar(5,5))
--------------------------

com as arrow functions é opcional colocar às chaves, podemos colocar diratamente o return
const somar = (x,y) => x+y;
console.log(somar(5,5));

--------------------------------
usando com um parâmetro
const contarLetra = nome => nome.length;
console.log(contarLetra('victor'));
----------------------------------

caso não tenha nenhum parâmetro, tem que se colocar os parênteses, e se a função for mais específica, tem que se colocar as chaves 
*/

